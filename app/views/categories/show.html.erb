<br>
<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<p>
  <strong>Total:</strong>
  <%= @category.total %>
</p>
<p>
  <strong>Total Current:</strong>
  <%= Spending.where(category: @category.id).sum(:price) %>
</p>
<br>
<%= link_to 'Edit', edit_category_path(@category), class: "button is-warning is-pulled-right" %>
<%= link_to 'Back', categories_path, class: "button is-dark" %>
